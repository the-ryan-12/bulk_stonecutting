package org.vee.bulk_stonecutting.client;

/// Lets a single checkbox draw its box at a custom size, since the vanilla box size is the same for every checkbox.
public interface ResizableCheckbox {
    void bulk_stonecutting$setBoxSize(int boxSize);
}
